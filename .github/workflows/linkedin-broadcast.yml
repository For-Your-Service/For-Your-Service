name: Daily LinkedIn Telemetry Broadcast

on:
  schedule:
    # Runs daily at 21:00 UTC (4:00 PM Central / 5:00 PM Eastern)
    - cron: '0 21 * * *'
  workflow_dispatch:

jobs:
  broadcast-to-linkedin:
    name: Broadcast Live Metrics to LinkedIn
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pytest

      - name: Broadcast Dynamic Telemetry to LinkedIn
        env:
          LINKEDIN_ACCESS_TOKEN: ${{ secrets.LINKEDIN_ACCESS_TOKEN }}
          LINKEDIN_AUTHOR_URN: ${{ secrets.LINKEDIN_AUTHOR_URN || secrets.LINKEDIN_PERSON_URN }}
        run: |
          python scripts/broadcast_linkedin_update.py
