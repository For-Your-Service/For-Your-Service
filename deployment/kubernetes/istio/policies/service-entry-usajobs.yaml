apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: usajobs-external
spec:
  hosts:
  - data.usajobs.gov
  ports:
  - number: 443
    name: https
    protocol: HTTPS
  resolution: DNS
  location: MESH_EXTERNAL
